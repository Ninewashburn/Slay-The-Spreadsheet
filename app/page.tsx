import CombatScreen from '@/components/combat/CombatScreen';

/**
 * La page d'accueil EST le combat (V0.5). L'écran d'accueil sobre
 * (logo texte, Jouer) arrive avec la tâche 4 de la roadmap — la run.
 */
export default function Home() {
  return <CombatScreen />;
}
